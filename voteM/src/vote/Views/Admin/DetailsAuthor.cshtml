@model IEnumerable<Author>
<div class="container-fluid">
    <div class="row">
        <div class="col-lg-8 col-lg-offset-2">
            <div>
                <h3>作者管理</h3>
            </div>
            <table class="table table-hover">
                <thead>
                    <tr>
                        <th>序号</th>
                        <th>作者</th>
                        <th>Email</th>
                        <th>操作</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var x in Model)
                    {
                        <tr id="table-tr">
                            <th>@x.Id</th>
                            <th>@x.AuthorName</th>
                            <th>@x.Email</th>
                            @if (User.IsSignedIn())
                            {
                                <td>
                                    <a asp-action="EditAuthor" asp-controller="Admin" asp-route-id="@x.Id">修改</a>
                                    <a asp-controller="Admin" asp-action="CreatePhotos">添加图片作品</a>
                                    <a href="javascript:remove(@x.Id)">删除</a>
                                </td>
                            }
                        </tr>
                    }
                </tbody>
            </table>
           <!-- Html.MakePager("pager-item", "active", "pager")-->
        </div>
    </div>
</div>
@if (User.IsSignedIn())
{
    <script src="~/scripts/jquery.min.js"></script>
    <script type="text/javascript">
        function remove(id) {
            var jug = confirm("确认删除？");
            if (jug == true) {
                $(function () {
                    var url = "/Admin/DeleteAuthor";
                    var params = { "id": id };
                    $.post(url, params, function (data) {
                        if (data == "success") {
                            $("#table-tr").remove();
                            alert("删除成功");

                        }
                        else {
                            alert("删除失败");
                        }

                    });
                });
            }
        }
    </script>
}
